// Simplified test version
export default function AdminOverview() {
    return (
        <div style={{ padding: '20px', color: 'white', backgroundColor: 'red' }}>
            <h1 style={{ fontSize: '48px', fontWeight: 'bold' }}>ADMIN DASHBOARD TEST</h1>
            <p>If you can see this, the routing works but there's a rendering issue with the main component.</p>
        </div>
    );
}
